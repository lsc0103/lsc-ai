###############################################################################
# LSC-AI Platform - openEuler 环境变量配置
# 复制此文件为 .env 并修改所有密码和密钥
###############################################################################

# ============== Database Configuration ==============
DB_USER=lscai
DB_PASSWORD=lscai123                  # ⚠️ 生产环境必须修改
DB_NAME=lscai

# ============== Redis Configuration ==============
REDIS_PASSWORD=redis123               # ⚠️ 生产环境必须修改

# ============== MinIO Configuration ==============
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin123          # ⚠️ 生产环境必须修改
MINIO_BUCKET=lscai

# ============== JWT Configuration ==============
JWT_SECRET=your-super-secret-jwt-key-change-in-production  # ⚠️ 生产环境必须修改为强随机密钥
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production  # ⚠️ 生产环境必须修改
JWT_REFRESH_EXPIRES_IN=7d

# ============== DeepSeek LLM API（公司内网）==============
DEEPSEEK_API_KEY=your-internal-api-key-here              # ⚠️ 替换为实际的 API Key
DEEPSEEK_BASE_URL=http://10.18.55.233:30069/deepseek_v3/chi/v1

# ============== Credential Encryption ==============
# 用于加密存储用户凭据（如 API 密钥、密码等）
CREDENTIAL_SALT=your-credential-salt-32-characters      # ⚠️ 必须修改为 32 字符随机字符串
CREDENTIAL_KEY=your-credential-encryption-key-here      # ⚠️ 必须修改为强随机密钥

# ============== Application Configuration ==============
LOG_LEVEL=info
WEB_PORT=80

# ============== 生成强随机密钥的方法 ==============
# 在 Linux 服务器上运行以下命令生成随机密钥：
#
# JWT_SECRET:
#   openssl rand -base64 64
#
# JWT_REFRESH_SECRET:
#   openssl rand -base64 64
#
# CREDENTIAL_SALT (32字符):
#   openssl rand -base64 24
#
# CREDENTIAL_KEY:
#   openssl rand -base64 32
