{
  "name": "lsc-ai",
  "version": "0.1.0",
  "description": "LSC AI - AI Coding demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -F @lsc-ai/cli dev",
    "build": "pnpm -r build",
    "start": "pnpm -F @lsc-ai/cli start",
    "lint": "eslint packages --ext .ts,.tsx",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.27.0",
  "dependencies": {
    "mysql2": "^3.16.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "tsx": "^4.21.0"
  }
}
